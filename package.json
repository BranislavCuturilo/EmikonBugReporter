{
  "name": "emikon-bug-reporter",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "description": "Chrome MV3 extension. `type: module` is for `node --test` only -- Chrome loads src/lib/*.js as ES modules regardless and ignores this file.",
  "scripts": {
    "test": "node --test \"tests/*.test.js\""
  }
}
